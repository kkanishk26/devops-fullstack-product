services:
  backend:
    image: devops-product-backend
    ports:
      - "5000:5000"
    restart: always

  frontend:
    image: devops-product-frontend
    ports:
      - "8082:80"
    depends_on:
      - backend
    restart: always
